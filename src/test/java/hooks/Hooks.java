package hooks;

import io.cucumber.java.Before;

public class Hooks {
    //here we may create initialisation of something (url, getting keys... etc)
    @Before
    public static void startSession() {

    }
}
